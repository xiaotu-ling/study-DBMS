一.存储过程:
     存储过程就是一组经过预先编译的SQL语句的封装.
     执行过程 : 存储过程==预先存储==到MySQL服务器上,需要执行时,==客户端==只需要==向服务器端==发出调用存储过程的命令,==服务器端==就可以==把预先存储好的==一系列==SQL语句全部执行==
     ![[Pasted image 20260812133701.png]]
     1.分类
         ![[Pasted image 20260812134005.png]]
     2.创建存储过程
         CREATE PROCEDURE 存储过程名(IN/OUT/INOUT 参数名 参数类型, ......)
         [ characteristics ...]
         BEGIN
             存储过程体
         END
     3.characteristics的取值:
         ![[Pasted image 20260812134615.png]]
         ![[Pasted image 20260812134638.png|547]]
         ![[Pasted image 20260812134659.png|551]]
         ![[Pasted image 20260812134750.png|555]]
         ![[Pasted image 20260812134801.png|483]]
     4.DELIMITER
         ![[Pasted image 20260812135137.png]]
         ![[Pasted image 20260812135223.png]]
         如果没有DELIMITER,运行上面中间的语句会报错,因为在emps后面有分号,表示执行结束,但是后面还有语句没执行,这时候引入==DELIMITER 加一个符号==表示==从这一行往下==,==结束符号是==你刚刚==跟在DELIMITER后的符号==,运行完后再加==DELIMIETER ;==让结束符号==变回分号==方便下面语句正常书写.
     5.举例(无参数无返回值):
        5.1创建存储过程select_all_data(),查看 emps表中的所有数据
             DELIMITER $
             CREATE  PROCEDURE select_all_data()
             BEGIN
                 SELECT * FROM emps;
             END $
             DELIMITER ;
             ![[Pasted image 20260812140527.png]]
             调用存储过程(没有参数):
                 CALL select_all_data();
                 ![[Pasted image 20260812140830.png|479]]
        5.2创建一个存储过程avg_employee_salary(),返回所有员工的平均工资
             ![[Pasted image 20260812141101.png]]
        5.3创建存储过程show_max_salary(),用来查看emps表中最高薪资.
             ![[Pasted image 20260812141427.png]]
     6.举例(带OUT和IN):
          6.1创建存储过程show_min_salary(),查看emps表中的最低薪资,并将最低薪资通过OUT参数"ms"输出
          ==这个ms是要求的参数名字,参数的数据类型要看OUT的是哪个值,这里是salary,要和原表里的对应数据的数据类型一样==
          ![[Pasted image 20260812142230.png]]
          ==有输出==,就要==把查询的值INTO给对应数据==,也就是上面的INTO ms
          调用时要在括号里写@加形参,@表示这是用户定义的参数,整体表示参数
          最后想看结果就直接查询这个参数,参数存储着结果.
          6.2创建存储过程show_someone_salary(),查看emps表中某个员工的薪资,并用IN参数empname输入员工姓名.
          ![[Pasted image 20260812143016.png]]
          6.3创建存储过程show_smone_salary2(),查看emps表中某个员工的薪资,并用IN参数empname输入员工姓名,用OUT参数empsalary输出员工薪资
          ![[Pasted image 20260812143913.png|546]]
     7.举例(带INOUT):
         创建存储过程show_mgr_name(),查询某个员工领导的名字,并用INOUT参数empname输入员工名字,输出领导名字.
         ![[Pasted image 20260812145829.png]]