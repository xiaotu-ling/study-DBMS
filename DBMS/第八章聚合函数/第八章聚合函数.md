==SELECT-FROM-WHERE-GROUP BY-HAVING-ORDER BY-LIMIT==
一.聚合函数介绍：
    聚合函数又叫聚集函数或者分组函数，是对一组数据进行汇总的函数，==输入一组数据输出单个结果==。
二.==常见==的聚合函数：
     1.AVG/SUM==适用于数值类型==：
         SELECT AVG(salary),SUM(salary)
         FROM employees;
         ![[屏幕截图 2026-07-23 172125.png]]
         字符串求和，求平均无效，要么==报错==要么==返回0==。
     2.MAX/MIN==适用于数值类型，字符串类型，日期时间类型==：
         SELECT MAX(salary),MIN(salary)
         FROM employees;
         ![[屏幕截图 2026-07-23 172352.png]]
          SELECT MAX(last_name),MIN(last_name)\
          FROM employees;
          ![[屏幕截图 2026-07-23 172650.png]]
     3.COUNT：
         ①作用：==计算指定字段在查询结果中出现的个数==
             SELECT COUNT(employee_id),COUNT(salary)
             FROM employees;
             ![[屏幕截图 2026-07-23 173020.png]]
             计数和括号里的值是多少没关系，只算有多少列
         ②==想要一定能计算表中有多少条数据有几种方法==
             方法一：COUNT( * )
             方法二：COUNT( 1 )
         ③==有NULL值的计算==
            SELECT AVG(salary),SUM(salary)/COUNT(salary),AVG(commission_pct),SUM(commission_pct)/COUNT(commission_pct),SUM(commission_pct)/107
            FROM employees;![[屏幕截图 2026-07-23 174153.png]]
            ==问题是怎么求公司中平均奖金率？==
             SELECT SUM(commission_pct)/COUNT(IFNULL(commission_pct,0))
             FROM employees;
             ![[屏幕截图 2026-07-23 174858.png]]
             在这里，IFNULL会==判断每一个人==的commission_pct是不是NULL值，然后正常是要生成一个表每一行==表示一个人==的commission_pct，之后==COUNT就把人数总和输出==了，对应了上面==②==的内容。如果直接COUNT，==COUNT不计算空值==，会不准确。
             或者：
             SELECT AVG(IFNULL(commission_pct,0))
             FROM employees;
             也可以。
          ④如果需要统计表中的记录数，使用COUNT(1),COUNT( * ),COUNT(具体字段)，这里默认具体字段是可以统计表中所有记录的，哪一种效率更高？
             如果使用M有ISAM存储引擎，则三者效率相同
             如果使用InnoDB存储引擎，则COUNT( * )>COUNT(1)>COUNT(具体字段)
     4.GROUP BY：
         ①需求1：==查询各个部门的平均工资，最高工资==
         SELECT department_id,AVG(salary),MAX(salary)
         FROM employees
         GROUP BY department_id;
         ![[屏幕截图 2026-07-24 200246.png]]
         ②需求2：==查询各个部门内相同工种的平均工资==
         SELECT department_id,job_id,AVG(salary)
         FROM employees
         GROUP BY department_id,job_id;
         ![[屏幕截图 2026-07-24 200551.png]]
         ③==错误例子==：
         SELECT department_id,==job_id==,AVG(salary)
         FROM employees
         GROUP BY department_id
         按照部门编号分组，一行只有一整个部门的信息，部门里不止一个人的，在MYSQL里，结果会只显示了一个人的job_id，这明显是错的。
         所以：==SELECT语句里除了聚合函数之外的都要在GROUP BY里出现==，GROUP BY里出现的语句可以不出现在SELECT语句里。
         ==拓展：SELECT-FROM-WHERE-GROUP BY-HAVING-ORDER BY-LIMIT==
     5.HAVING:用来过滤数据,==过滤条件使用了聚合函数==的==用HAVING==来==过滤==
               ==HAVING在GROUP BY后面==
               没有GROUP BY，用HAVING过滤没有什么意义。
         需求：部门中最高工资比10000高的部门信息
         SELECT department_id,MAX(salary)
         FROM employees
         GROUP BY department_id
         HAVING MAX(salary) > 10000;
         ![[屏幕截图 2026-07-25 134956.png]]
         ==练习==:查询部门id为10，20，30，40这四个部门中最高工资比10000高的部门信息。
         SELECT department_id,MAX(salary)
         FROM employees
         WHERE department_id IN  (10,20,30,40)
         GROUP BY department_id
         HAVING MAX(salary) > 10000;
         ![[屏幕截图 2026-07-25 141450.png]]
     6.WHERE 与 HAVING 的对比：
         ① 从适用范围来看，HAVING 的适用范围更广。
         ② 如果过滤条件中没有聚合函数，WHERE的执行效率高于HAVING。
     7.SQL底层执行原理：
         ① ==SELECT 语句的完整结构==
             ==SQL92语法==：
                SELECT ... ... ... ...(存在聚合函数)
                FROM... ... ... ...
                WHERE 多表的链接条件/不包含聚合函数的过滤条件
                GROUP BY ... ...
                HAVING 包含聚合函数的过滤条件
                ORDER BY ... ... ...(ASC / DESC)
                LIMIT ...,...
             ==SQL99语法==:
                SELECT ... ... ... ...(存在聚合函数)
                FROM... (LEFT/RIGHT )JOIN ... ON 多表连接条件 JOIN ... ON ...
                WHERE 多表的链接条件/不包含聚合函数的过滤条件
                GROUP BY ... ...
                HAVING 包含聚合函数的过滤条件
                ORDER BY ... ... ...(ASC / DESC)
                LIMIT ...,...
         ② ==SELECT 语句的执行过程==：
             FROM ... , ... ==->== ON ==->== (LEFT/RIGHT   JOIN ) ==->== WHERE ==->== GROUP BY ==->== HAVING ==->== SELECT ... , ... , ...(包含聚合函数) ==->== ORDER BY (ASC/DESC) ==->== LIMIT ... , ...
             ==先看==是要==用哪些表==，==然后多表连接==，==再看==看是不是要进行==左/右外连接==，之后看要==筛选什么条件==，然后看筛选后的数据==需不需要分组==，如果分组看==分组后筛选什么条件==，==之后==再进行==查询==操作看==要展示哪些列==，==最后==是看==排序==问题==以及==分页。
三.课后习题：
     1.where子句可否使用聚合函数进行过滤?
         不可以
     2.查询公司员工工资的最大值，最小值，平均值，总和
         SELECT MAX(salary),MIN(salary),AVG(salary),SUM(salary)
         FROM employees;
         ![[屏幕截图 2026-07-25 163144.png]]
     3.查询各job_id的员工工资的最大值，最小值，平均值，总和
         SELECT job_id,MAX(salary),MIN(salary),AVG(salary),SUM(salary)
         FROM employees
         GROUP BY job_id;
         ![[Pasted image 20260725163338.png]]
     4.选择具有各个job_id的员工人数
         SELECT job_id,COUNT( * )
         FROM employees
         GROUP BY job_id;
         ![[Pasted image 20260725163652.png]]
    5.查询员工最高工资和最低工资的差距(DIFFERENCE）
         SELECT MAX(salary) - MIN(salary) DIFFERENCE
         FROM employees;
         ![[Pasted image 20260725164025.png]]
    6.查询各个管理者手下员工的最低工资，其中最低工资不能低于6000.没有管理者的员工不计算在内
         SELECT manager_id,MIN(salary)
         FROM employees 
         WHERE manager_id IS NOT NULL
         GROUP BY manager_id
         HAVING MIN(salary) > 6000; 
         ![[Pasted image 20260725164521.png]]
     7.查询所有部门的名字，location_id，员工数量和平均工资，并按平均工资降序
         SELECT dep.department_name,dep.location_id,COUNT( employee_id ),AVG(salary)
         FROM employees em JOIN department dep ON em.department_id = dep.department_id
         GROUP BY dep.department_name
         ORDER BY AVG(salary) DESC;
         
    
     